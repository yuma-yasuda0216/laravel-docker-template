# Laravel Lesson レビュー①

🔹## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
A.SELECT * FROM todos;
### Todoモデルのallメソッドの返り値は何か
A.Collectionクラス
### 配列の代わりにCollectionクラスを使用するメリットは
A.CollectionクラスはLalavelが元々用意しているフレームワークで、使用することで対象のテーブル内にあるレコード全て取得することができる
### view関数の第1・第2引数の指定と何をしているか
A.第一引数はファイルのパスを指定し、第二引数はテーブル内の情報を指定しています。指定することで画面に表示されます
### index.blade.phpの$todos・$todoに代入されているものは何か
A.$todosはControllerクラスで取得した情報が代入されていて、$todoは対象のテーブル内のレコードを格納されています

🔹## Todo作成機能

### Requestクラスのallメソッドは何をしているか
A.フォームから入力したデータを送信し、送信したデータはレコードとして呼び出している
### fillメソッドは何をしているか
A.画面から送られてきたデータをモデルへ一括で代入している
### $fillableは何のために設定しているか
A.攻撃者対策として設定されています。中身は対象のテーブル内のカラム名を設定しています。
### saveメソッドで実行しているSQLは何か
A.フォームから入力したデータはエロクウェイトを通して、インサイト文として実行している
### redirect()->route()は何をしているか
A.リダイレクト送信をするための記述で、routeメソッドの引数にパスを指定することでリダイレクト送信するヘルパー関数です

🔹## その他

### テーブル構成をマイグレーションファイルで管理するメリット
A.DBのテーブル名、カラム名、削除をマイグレーションファイル内で直接作成することができるので、開発チームで共有することができる
### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
A.upメソッドはphp artisan migrate、downメソッドはphp artisan migrate:rollback
### Seederクラスの役割は何か
A.データベースのテスト用データを作成する役割
### route関数の引数・返り値・使用するメリット
A.引数は「web.php」ファイルのnameメソッドで設定したパスで、返り値はhttpから始まるのリンクです。メリットはもしURLを書き変える必要があった場合、indexファイルの引数とルートファイルから修正することができるため、保守性が向上する
### @extends・@section・@yieldの関係性とbladeを分割するメリット
A.@extendsは子ファイル側で引数に親ファイルのパスを入れることで親ファイルに記載されているコードが子ファイルの@extendsに渡されます。@sectionと@yieldは引数に同じ文字列を記述することで親ファイル側の@yieldが子ファイルの@section~@endsectionまでのコードを表示するリファクタリングです。
メリットはブレードファイルが複数あったときに毎回同じ記述をしないといけないため、まとめるためのブレードファイルを作成することでコードが見やすくなる
### @csrfは何のための記述か
A.攻撃者対策のディレクティブです。記述をすることで、送信元からフォームの送信を押したと同時にトークンIDが発行され、許可をとります。
### {{ }}とは何の省略系か
A.ブレードファイルで使用するechoです。記述することでPHPの処理内容が出力されます。